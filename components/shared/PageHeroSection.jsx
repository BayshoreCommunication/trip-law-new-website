import React from "react";
import SectionLayout from "./SectionLayout";

const PageHeroSection = ({ image }) => {
  return <SectionLayout bg={image}>PageHeroSection</SectionLayout>;
};

export default PageHeroSection;
